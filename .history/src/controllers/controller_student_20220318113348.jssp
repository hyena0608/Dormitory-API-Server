define opcode[controller=ApiStudent, base='/api/v1']
{
    [method=post, action='student/insert', noauth, overwrite=true]
    insertStudent 3001 '학생 저장'

    [method=post, action='student/update', noauth, overwrite=true]
    updateStudent 3002 '학생 수정'

    [method=post, action='student/delete', noauth, overwrite=true]
    deleteStudent 3003 '학생 삭제'

    [method=get, action='student/select', noauth, overwrite=true]
    selectStudentOneByIdHakbun 3004 '학생 개별 조회'

    [method=get, action='student/select/list', noauth, overwrite=true]
    selectStudentListBySexNameAge 3005 '학생 리스트 조회'

    [method=post, action='student/insert/room', noauth, overwrite=true]
    insertStudentRoom 3006 '학생 방 입실 신청'
}

/**
 * 001
 */
req insertStudent {
    studentHakbun Long;
    studentSex
}

res insertStudent {
}

/**
 * 1002
 */
req updateStudent {
}

res updateStudent {
}

/**
 * 1003
 */
req deleteStudent {
}

res deleteStudent {
}

/**
 * 1004
 */
req selectStudentOneByIdHakbun {
}

res selectStudentOneByIdHakbun {
}

/**
 * 1005
 */
req selectStudentListBySexNameAge {}

res selectStudentListBySexNameAge {
}

/**
 * 1006
 */
req insertStudentRoom {}

res insertStudentRoom {
}